# Spravka — production. Four apps + their own MySQL.
#
#   docker compose up -d --build
#
# CO-HOSTED with qrcode-pro on this one server: all six names resolve here, so only one nginx can
# own :443. spravka-nginx is that single edge — it serves the four spravka apps AND fronts
# bright.qrsystem.uz, proxying to qrcode-app and serving qrcode's qr/uploads off disk exactly the
# way qrcode-nginx did. So this compose reaches INTO the qrcode-pro stack: its network
# (qrcode-pro_qrnet) and two of its volumes are declared `external` below, which means qrcode-pro
# must already be up. The only thing that changes in qrcode is that qrcode-nginx is retired — its
# app, database and volumes are untouched.
#
# The monorepo's web-qr is deliberately absent: qrcode-app already serves that role, and running a
# second copy against this empty database would 404 every QR ever printed.
#
# .env (git-ignored) supplies the secrets. See .env.docker.example.

x-app: &app
  image: spravka:latest
  build:
    context: .
    args:
      # Inlined by `next build`, so it belongs here and not in `environment:` — see the Dockerfile.
      NEXT_PUBLIC_PUBLIC_URL: ${NEXT_PUBLIC_PUBLIC_URL:-https://qrsystem.uz}
  restart: unless-stopped
  env_file: .env
  environment:
    # `mysql` is the service name below; inside the network it is 3306, not the host's 3306.
    DATABASE_URL: "mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-spravka}"
  depends_on:
    mysql:
      condition: service_healthy
  networks: [spravka]

services:
  mysql:
    image: mysql:8.4
    container_name: spravka-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-spravka}
    # No `ports:`. Nothing outside this network needs the database, and Docker's published ports
    # write their own iptables rules that bypass ufw — a published 3306 is open to the LAN even
    # with the firewall "on". The dev compose publishes 3310; that file is for laptops.
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 5s
      timeout: 5s
      retries: 30
    networks: [spravka]

  # The page a printed QR opens. Its URL is burned into paper — hence the apex domain, and hence
  # it reads the same frozen PDFs the rahbar wrote.
  public:
    <<: *app
    container_name: spravka-public
    environment:
      DATABASE_URL: "mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-spravka}"
      APP: public
      PORT: 5100
      CERT_STORAGE_DIR: /var/lib/spravka/storage
    ports: ["127.0.0.1:5100:5100"]
    volumes:
      - cert_storage:/var/lib/spravka/storage

  yurist:
    <<: *app
    container_name: spravka-yurist
    environment:
      DATABASE_URL: "mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-spravka}"
      APP: yurist
      PORT: 5101
    ports: ["127.0.0.1:5101:5101"]

  admin:
    <<: *app
    container_name: spravka-admin
    environment:
      DATABASE_URL: "mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-spravka}"
      APP: admin
      PORT: 5102
      CERT_STORAGE_DIR: /var/lib/spravka/storage
    ports: ["127.0.0.1:5102:5102"]
    volumes:
      - cert_storage:/var/lib/spravka/storage

  # Signing happens here: Chromium freezes the PDF, and E-IMZO runs on the rahbar's own machine.
  rahbar:
    <<: *app
    container_name: spravka-rahbar
    environment:
      DATABASE_URL: "mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-spravka}"
      APP: rahbar
      PORT: 5103
      CERT_STORAGE_DIR: /var/lib/spravka/storage
    ports: ["127.0.0.1:5103:5103"]
    volumes:
      - cert_storage:/var/lib/spravka/storage
    # Chromium's default /dev/shm is 64 MB in Docker and it crashes mid-render on a big page.
    # render.ts already passes --disable-dev-shm-usage, which is the belt; this is the braces.
    shm_size: 512m

  # TLS terminates here. Host 80/443 must be free — check with:
  #   sudo ss -ltnp | grep -E ':80 |:443 '
  # A host nginx holding them: sudo systemctl disable --now nginx
  nginx:
    image: nginx:1.27-alpine
    container_name: spravka-nginx
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./deploy/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/nginx/snippets:/etc/nginx/snippets:ro
      - ./deploy/nginx/certs:/etc/nginx/certs:ro
      - certbot_www:/var/www/certbot
      # qrcode's runtime-written files, served straight off disk for bright.qrsystem.uz. Next
      # standalone does not serve runtime public files, so — exactly as qrcode-nginx did — nginx
      # does. Read-only: this edge reads qrcode's QR images, it never writes them.
      - qrcode_qr:/srv/qr:ro
      - qrcode_uploads:/srv/uploads:ro
    # The reload loop is not decoration. certbot below renews the files and stops there; nginx
    # reads its certificate once at start and keeps serving that copy from memory. Without this,
    # renewal succeeds every 60 days, the logs say so, and on day 90 every site warns that the
    # certificate expired — with a valid one sitting on disk. Silent, months out, and reported as
    # working. (credit-core's compose is missing exactly this line.)
    command: /bin/sh -c 'while :; do sleep 6h & wait $${!}; nginx -s reload; done & nginx -g "daemon off;"'
    depends_on: [public, yurist, admin, rahbar]
    # qrnet too, so it can reach qrcode-app:5000 for bright.qrsystem.uz.
    networks: [spravka, qrnet]

  # Issued once by deploy/init-letsencrypt.sh, renewed here every 12h. certbot exits quietly when
  # there is nothing due, so the loop is cheap.
  certbot:
    image: certbot/certbot
    container_name: spravka-certbot
    restart: unless-stopped
    volumes:
      - ./deploy/nginx/certs:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    entrypoint: /bin/sh -c 'trap exit TERM; while :; do certbot renew --webroot -w /var/www/certbot; sleep 12h & wait $${!}; done;'
    networks: [spravka]

volumes:
  mysql_data:
  # ⚠ ISSUED LEGAL DOCUMENTS, not a cache. A file here cannot be re-made — re-rendering produces
  # different bytes and a different pdfSha256, which breaks the link between the paper someone
  # holds and the row in the database. Back this up wherever the database is backed up.
  #   docker run --rm -v spravka_cert_storage:/s -v $PWD:/b alpine tar czf /b/storage.tgz -C /s .
  cert_storage:
  certbot_www:
  # From the qrcode-pro stack, borrowed read-only so bright.qrsystem.uz serves the same QR images
  # and uploads it always did. Created by qrcode-pro's compose — this one only mounts them.
  qrcode_qr:
    external: true
    name: qrcode-pro_qr_data
  qrcode_uploads:
    external: true
    name: qrcode-pro_uploads_data

networks:
  spravka:
  # qrcode-pro's own network, so spravka-nginx can resolve and reach qrcode-app:5000. `external`
  # means this compose does not create it — qrcode-pro must already be up (`docker compose up` in
  # the qrcode-pro directory) or spravka-nginx will refuse to start.
  qrnet:
    external: true
    name: qrcode-pro_qrnet
